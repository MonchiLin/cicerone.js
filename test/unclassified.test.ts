import {describe, expect, it} from "vitest";
import {toArea} from "../src/index";

describe("add", () => {
  it("should sum of 2 and 3 equals to 5", () => {
    expect(toArea("")).toEqual(null);
  });
});
